資料庫

主題：

使用者
編號(int)
名稱(string)
電話（string)
Email(string)

影片
編號(int)
片名(string)
類型(string)
片長（string)
上架日期（string)

Create database platform character set utf8 collate utf8_general_ci;

Use platform ;

CREATE TABLE user
(
    userid  Int Not Null,
    name varchar(100),
    number varchar(100),
    email varchar(100)
);

Use platform ;

CREATE TABLE video
(
    videoid  Int NOT NULL ,
    name varchar(100),
    `type`varchar(100),
    time varchar(100),
    date varchar(100)
);